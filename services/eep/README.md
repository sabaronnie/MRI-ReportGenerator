# EEP Service

This package is the public-facing orchestration layer for the project.

Intended responsibilities:

- accept a case submission
- call segmentation
- call measurements
- call interpretation
- assemble or trigger reporting

The current `app.py` is a lightweight scaffold so the repo structure matches the intended architecture.
