flask>=3.0
prometheus_client>=0.20
fpdf2>=2.7
# builder.py imports services.interpretation (pure-Python thresholds); the HTML renderers
# use stdlib only; the clinical PDF renderer uses fpdf2 (pure-Python, no system deps).
