[pytest]
# Repo root is on sys.path so `services.*` imports resolve.
testpaths = services/eep/tests tests/integration tests/e2e services/reporting/tests
markers =
    e2e: hits the deployed system (needs EEP_BASE_URL); skipped otherwise
    integration: exercises more than one component
filterwarnings =
    ignore::DeprecationWarning
